#ifndef __IDT_H
#define __IDT_H

#include "common.h"

typedef struct IDTDescriptor_t
{

    uint16 offset_first;
    uint16 selector;
    uint8  zero;
    uint8  type_attrib;
    uint16 offset_second;

} IDTDescriptor;

#endif